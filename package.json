{
  "name": "coffee-shop-pos-system",
  "version": "1.0.0",
  "description": "Hệ thống Quản lý Quán Cà Phê (Fullstack Cafe POS)",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"bgBlue.bold,bgMagenta.bold\" \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "start": "concurrently -n \"BACKEND,FRONTEND\" -c \"bgBlue.bold,bgMagenta.bold\" \"npm --prefix backend start\" \"npm --prefix frontend run dev\"",
    "install:all": "npm install && npm --prefix backend install && npm --prefix frontend install",
    "build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "@vercel/speed-insights": "^2.0.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.6",
    "express": "^4.22.2",
    "jsonwebtoken": "^9.0.3"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
